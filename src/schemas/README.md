# Content Schemas

The JSON files document the external content contracts from `CONTENT_SCHEMA.md`.
Runtime validation is implemented in `content-validation.ts` so the repository can validate authored JSON and Markdown without adding a runtime schema dependency.
